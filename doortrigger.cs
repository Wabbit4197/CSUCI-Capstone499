using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class doortrigger : MonoBehaviour
{
    // Start is called before the first frame update
       public door GetDoortype;

    // a script detect if the player has the corresponding key, set circle collider on near by 4 tile, if yes unlock the box collider, if not keep the collider.
}
